## Local environment variables (copy to .env)
#
# Appium server URL (local or CI runner)
APPIUM_SERVER_URL=http://127.0.0.1:4723
#
# Optional: set a friendly name (Appium uses `udid` for actual binding)
APPIUM_DEVICE_NAME=Android Emulator
#
# Optional: force a specific device serial (otherwise auto-detected via `adb devices`)
# Examples: emulator-5554, R58M12345AB, 192.168.56.101:5555
APPIUM_UDID=
